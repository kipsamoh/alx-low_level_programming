#include "main.h"

/**
 * _abs - calculates absolute value of int
 * @b: interger to determine
 * Return: absolute value of interger
 */
int _abs(int b)
{
	if (b >= 0)
	{
		return (b);
	}
	return (-b);
}
